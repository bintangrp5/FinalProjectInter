// import { Router } from "express";
// import { getAllAuthors, getAuthorById, createAuthors, updateAuthorById, deleteAuthorById  } from "../controllers/authorController";


// const authorRouter = Router()

// authorRouter.get('/', getAllAuthors)
// authorRouter.get('/:id', getAuthorById)
// authorRouter.post('/', createAuthors)
// authorRouter.put('/:id', updateAuthorById)
// authorRouter.delete('/:id', deleteAuthorById)

// export default authorRouter;